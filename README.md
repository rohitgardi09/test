# Exception Tracker Utility

## Overview

Exception Tracker Utility is a reusable Spring Boot utility used to capture and store application exceptions into Oracle database automatically using AOP.

--------------------------------------------------------------------------------

# Features

- AOP based exception tracking
- Custom annotation support
- Async DB logging
- Oracle DB support
- MDC value capture
- Stack trace capture
- Reusable utility jar
- Production ready utility

--------------------------------------------------------------------------------

# Project Structure

com.sbi.epay.exceptionTracker
│
├── annotation
│   └── TrackException
│
├── aspect
│   └── ExceptionTrackerAspect
│
├── config
│   └── AsyncConfig
│
├── dao
│   └── ExceptionLogDao
│
├── dto
│   └── ExceptionLogDto
│
├── entity
│   └── ExceptionLog
│
├── mapper
│   └── ExceptionLogMapper
│
├── repository
│   └── ExceptionLogRepository
│
├── service
│   ├── ExceptionAsyncService
│   └── ExceptionLogService
│
├── util
│   ├── MDCUtil
│   ├── StackTraceUtil
│   └── ExceptionUtil

--------------------------------------------------------------------------------

CREATE TABLE EXCEPTION_LOG (

    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SERVICE_NAME VARCHAR2(100),
    CLASS_NAME VARCHAR2(500),
    METHOD_NAME VARCHAR2(500),
    EXCEPTION_TYPE VARCHAR2(500),
    EXCEPTION_MESSAGE CLOB,
    STACK_TRACE CLOB,
    MID VARCHAR2(100),
    ORDER_REF VARCHAR2(200),
    ATRN VARCHAR2(200),
    PAYMODE VARCHAR2(100),
    TRACE_ID VARCHAR2(200),
    REQUEST_ID VARCHAR2(200),
    MDC_JSON CLOB,
    CREATED_AT NUMBER

);

--------------------------------------------------------------------------------

implementation 'org.springframework.boot:spring-boot-starter-aop'

implementation 'org.springframework.boot:spring-boot-starter-data-jpa'

implementation 'com.fasterxml.jackson.core:jackson-databind'

implementation 'org.projectlombok:lombok'

implementation 'com.oracle.database.jdbc:ojdbc11'

--------------------------------------------------------------------------------

# Main Application Configuration

@SpringBootApplication(
    scanBasePackages = {
        "com.epay.merchant",
        "com.sbi.epay.exceptionTracker"
    }
)

--------------------------------------------------------------------------------

# Usage

@TrackException
public void processPayment() {

    int x = 10 / 0;
}

--------------------------------------------------------------------------------

# Flow

Controller/Service
        │
        ▼
@TrackException
        │
        ▼
ExceptionTrackerAspect
        │
        ▼
ExceptionAsyncService
        │
        ▼
ExceptionLogDao
        │
        ▼
Oracle DB

--------------------------------------------------------------------------------

# Stored Details

- Service Name
- Class Name
- Method Name
- Exception Type
- Exception Message
- Stack Trace
- MID
- ORDER_REF
- ATRN
- PAYMODE
- TRACE_ID
- REQUEST_ID
- MDC JSON
- Created Timestamp

--------------------------------------------------------------------------------

# Test API

@RestController
public class TestController {

    @GetMapping("/test-exception")
    @TrackException
    public String testException() {

        int x = 10 / 0;

        return "SUCCESS";
    }
}

--------------------------------------------------------------------------------

# Build Command

gradlew clean build

Generated Jar:

build/libs/exception-tracker-service-1.0.0.jar

--------------------------------------------------------------------------------

# Production Usage

This utility can be reused across multiple Spring Boot microservices for centralized exception tracking and monitoring.

--------------------------------------------------------------------------------

# Author

Rohit Gardi
